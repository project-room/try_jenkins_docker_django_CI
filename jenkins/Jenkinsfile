pipeline {
    agent none

    stages {
        stage('Build base image') {
            agent {
                docker {
                    image 'bash'
                }
            }
            steps {
                sh 'echo "Hello Jenkins"'
            }
        }
    }
}